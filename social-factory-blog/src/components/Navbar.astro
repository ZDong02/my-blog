<nav class="fixed top-0 left-0 right-0 z-50 bg-white/80 dark:bg-zinc-950/80 backdrop-blur-md border-b border-zinc-200 dark:border-zinc-800">
  <div
    class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between"
  >
    <a href="/" class="flex items-center gap-3">
      <span class="text-red-600 text-xl font-bold">壊れた心の人</span>
      <img
        src="/images/V1.jpg"
        alt="Social Factory Blog"
        class="h-10 w-auto border"
      />
    </a>

    <!-- Desktop Navigation -->
    <div class="hidden md:flex items-center gap-6">
      <a href="/about" class="text-sm uppercase tracking-widest hover:text-red-600 transition">ABOUT</a>
      <a href="/roadmap" class="text-sm uppercase tracking-widest hover:text-red-600 transition">ROADMAP</a>
      <a href="/faq" class="text-sm uppercase tracking-widest hover:text-red-600 transition">FAQ</a>
      <a href="/contact" class="text-sm uppercase tracking-widest hover:text-red-600 transition">CONTACT</a>
      <a href="/blog" class="text-sm uppercase tracking-widest hover:text-red-600 transition">BLOG</a>

      <!-- Search Box -->
      <form action="/search" method="GET" class="relative">
        <input
          type="text"
          name="q"
          placeholder="Search..."
          class="w-48 px-3 py-1.5 text-sm border border-gray-300 dark:border-zinc-700 rounded-full bg-white dark:bg-zinc-900 focus:outline-none focus:ring-2 focus:ring-red-500"
        />
        <button type="submit" class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 hover:text-red-600">
          <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
          </svg>
        </button>
      </form>
    </div>

    <!-- Mobile Menu Button -->
    <div class="md:hidden flex items-center gap-4">
      <button
        onclick="document.getElementById('mobile-menu').classList.toggle('hidden')"
        class="w-8 h-8 flex items-center justify-center text-xl">☰</button>
      <button
        onclick="document.documentElement.classList.toggle('dark')"
        class="w-8 h-8 flex items-center justify-center text-xl">🌗</button>
    </div>

    <!-- Desktop Auth/User Section -->
    <div class="hidden md:flex items-center gap-4">
      <!-- Auth links - shown when not logged in -->
      <div id="desktop-auth-links" class="flex items-center gap-4">
        <a href="/login" class="text-xs uppercase tracking-widest hover:text-red-600">LOGIN</a>
        <a href="/register" class="text-xs uppercase tracking-widest hover:text-red-600">REGISTER</a>
      </div>

      <!-- User info - shown when logged in -->
      <div id="desktop-user-info" class="hidden flex items-center gap-3">
        <a href="/dashboard" class="text-xs uppercase tracking-widest hover:text-red-600">DASHBOARD</a>
        <a id="desktop-admin-link" href="/admin/posts" class="hidden text-xs uppercase tracking-widest text-red-600 hover:text-red-500">ADMIN</a>
        <button id="desktop-logout-btn" class="text-xs uppercase tracking-widest hover:text-red-600">LOGOUT</button>
      </div>
    </div>
  </div>

  <!-- Mobile Navigation -->
  <div
    id="mobile-menu"
    class="hidden md:hidden bg-white dark:bg-zinc-950 border-t border-zinc-200 dark:border-zinc-800"
  >
    <div
      class="max-w-7xl mx-auto px-4 py-4 flex flex-col gap-4 text-sm uppercase tracking-widest"
    >
      <!-- Search Box (Mobile) -->
      <form action="/search" method="GET" class="flex gap-2">
        <input
          type="text"
          name="q"
          placeholder="Search..."
          class="flex-1 px-3 py-2 text-sm border border-gray-300 dark:border-zinc-700 rounded-lg bg-white dark:bg-zinc-900 focus:outline-none focus:ring-2 focus:ring-red-500"
        />
        <button type="submit" class="px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 transition">
          <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
          </svg>
        </button>
      </form>

      <a href="/about" class="hover:text-red-600 transition py-2">ABOUT</a>
      <a href="/roadmap" class="hover:text-red-600 transition py-2">ROADMAP</a>
      <a href="/faq" class="hover:text-red-600 transition py-2">FAQ</a>
      <a href="/contact" class="hover:text-red-600 transition py-2">CONTACT</a>
      <a href="/blog" class="hover:text-red-600 transition py-2">BLOG</a>

      <!-- Mobile Auth links - shown when not logged in -->
      <div id="mobile-auth-links" class="pt-2 border-t border-zinc-200 dark:border-zinc-800 flex gap-4">
        <a href="/login" class="hover:text-red-600 transition py-2">LOGIN</a>
        <a href="/register" class="hover:text-red-600 transition py-2">REGISTER</a>
      </div>

      <!-- Mobile User info - shown when logged in -->
      <div id="mobile-user-info" class="hidden pt-2 border-t border-zinc-200 dark:border-zinc-800 flex flex-col gap-2">
        <span id="mobile-username" class="text-xs uppercase tracking-widest text-red-600"></span>
        <a href="/dashboard" class="hover:text-red-600 transition py-2">DASHBOARD</a>
        <a href="/my-likes" class="hover:text-red-600 transition py-2">My Likes</a>
        <a href="/my-bookmarks" class="hover:text-red-600 transition py-2">Bookmarks</a>
        <a href="/my-comments" class="hover:text-red-600 transition py-2">Comments</a>
        <a href="/profile" class="hover:text-red-600 transition py-2">Profile</a>
        <a id="mobile-admin-link" href="/admin/posts" class="hidden text-red-600 hover:text-red-500 transition py-2">ADMIN PANEL</a>
        <button id="mobile-logout-btn" class="text-left hover:text-red-600 transition py-2">LOGOUT</button>
      </div>

      <div class="flex items-center gap-4 pt-2 border-t border-zinc-200 dark:border-zinc-800">
        <a href="#" class="text-xs uppercase tracking-widest hover:text-red-600">FB</a>
        <a href="#" class="text-xs uppercase tracking-widest hover:text-red-600">TW</a>
      </div>
    </div>
  </div>

  <script>
    // Auth state management
    function updateAuthUI(user, isAuthenticated) {
      const desktopAuthLinks = document.getElementById('desktop-auth-links');
      const desktopUserInfo = document.getElementById('desktop-user-info');
      const desktopAdminLink = document.getElementById('desktop-admin-link');
      const desktopLogoutBtn = document.getElementById('desktop-logout-btn');

      const mobileAuthLinks = document.getElementById('mobile-auth-links');
      const mobileUserInfo = document.getElementById('mobile-user-info');
      const mobileUsername = document.getElementById('mobile-username');
      const mobileAdminLink = document.getElementById('mobile-admin-link');
      const mobileLogoutBtn = document.getElementById('mobile-logout-btn');

      if (isAuthenticated && user) {
        // Show user info
        desktopAuthLinks.classList.add('hidden');
        desktopUserInfo.classList.remove('hidden');
        mobileAuthLinks.classList.add('hidden');
        mobileUserInfo.classList.remove('hidden');

        // Set username
        const displayName = user.nickname || user.username;
        desktopUsername.textContent = '@' + displayName;
        mobileUsername.textContent = displayName;

        // Show admin link if admin
        if (user.role === 'ADMIN') {
          desktopAdminLink.classList.remove('hidden');
          mobileAdminLink.classList.remove('hidden');
        } else {
          desktopAdminLink.classList.add('hidden');
          mobileAdminLink.classList.add('hidden');
        }

        // Logout handlers
        desktopLogoutBtn.onclick = handleLogout;
        mobileLogoutBtn.onclick = handleLogout;
      } else {
        // Show auth links
        desktopAuthLinks.classList.remove('hidden');
        desktopUserInfo.classList.add('hidden');
        mobileAuthLinks.classList.remove('hidden');
        mobileUserInfo.classList.add('hidden');
      }
    }

    async function handleLogout() {
      if (window.authManager) {
        await window.authManager.logout();
        window.location.href = '/';
      }
    }

    // Initialize auth state on page load
    document.addEventListener('DOMContentLoaded', () => {
      if (window.authManager) {
        window.authManager.addListener(({ user, isAuthenticated }) => {
          updateAuthUI(user, isAuthenticated);
        });

        // Initial state
        const authState = window.authManager.getAuthState();
        updateAuthUI(authState.user, authState.isAuthenticated);
      }
    });

    // Also update immediately if auth is already loaded
    if (window.authManager) {
      const authState = window.authManager.getAuthState();
      if (!authState.isLoading) {
        updateAuthUI(authState.user, authState.isAuthenticated);
      }
    }
  </script>
</nav>
